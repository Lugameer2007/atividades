#include <stdio.h>
#include <locale.h>

int main(){
    
int vetor[6], i, contadorImpar = 0, somaImpar = 0;
for(int i = 0, i < 6, i++){
    printf("Digite o %d valor:", i + 1);
    scanf("%d", &vetor[i]);
}
for(int i = 0, i < 6, i++){
    if(vetor[i]% 2 != 0)
    somaImpar += vetor[i];
}
printf("A soma dos numeros impares são: %d", Somaimpar);
}
